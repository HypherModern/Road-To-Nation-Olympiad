#include <bits/stdc++.h>

using namespace std;

int main(){

    int x, k;
    int S = 0, T = 0;
    int D[k] = {0};
    for (int i = 0; i < k; i++) {
        cin >> x;
        D[x]++;
    }
    for (int x = 10000; i > 0; i--){

        if D[x] > 0{

            T++;
            S += x;
            D[x]--;
            if (T == k) break;
            
        }

    }
    

}
